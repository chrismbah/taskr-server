describe("Server.ts tests", () => {
  test("Math test", () => {
    expect(2 + 2).toBe(4);
  });
});

describe("My test", () => {
  test("Multiplication test", () => {
    expect(3 * 33).toBeGreaterThan(2);
  });
});
